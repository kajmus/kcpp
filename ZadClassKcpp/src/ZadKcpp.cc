#include "../include/ZadKcpp.h"
// Ten plik może być w zasadzie pusty, jeśli wszystkie metody 
// w klasie nadrzędnej są czysto wirtualne (pure virtual). 
// Dodajemy go tylko po to, by spełnić wymogi zadania.